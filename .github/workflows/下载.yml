name: youtube

on:
  workflow_dispatch:
    inputs:
      video_url:
        description: 'Enter YouTube video URL'
        required: true
        default: 'https://youtu.be/sample_video'
      media_choice:
        description: '选择媒体类型 (视频、音频或视频+音频)'
        required: true
        type: choice
        options:
          - '720p'
          - '1080p'
          - '1440p'
          - '4K'
          - '8K'
          - '720p + Audio'
          - '1080p + Audio'
          - '4K + Audio'
          - '8K + Audio'
          - 'Audio Only'
        default: '1080p + Audio'

jobs:
  get_media:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Install yt-dlp and ffmpeg
        run: |
          pip install yt-dlp
          sudo apt-get update && sudo apt-get install -y ffmpeg
      
      - name: 获取视频和音频流编号
        run: |
          yt-dlp --cookies cookies.txt -F ${{ github.event.inputs.video_url }} > formats.txt
          MEDIA_CHOICE="${{ github.event.inputs.media_choice }}"

          # 如果用户选择了视频+音频
          if [[ "$MEDIA_CHOICE" == *"+ Audio"* ]]; then
            RES=$(echo "$MEDIA_CHOICE" | sed 's/ + Audio//')
            A=$(grep -E "${RES}" formats.txt | grep https | sort -k6 -nr | head -n 1 | awk '{print $1}')
            B=$(grep 'audio only' formats.txt | grep https | sort -k6 -nr | head -n 1 | awk '{print $1}')
            echo "A=$A" >> $GITHUB_ENV
            echo "B=$B" >> $GITHUB_ENV
          # 如果用户选择了仅音频
          elif [[ "$MEDIA_CHOICE" == "Audio Only" ]]; then
            C=$(grep 'audio only' formats.txt | grep https | sort -k6 -nr | head -n 1 | awk '{print $1}')
            echo "C=$C" >> $GITHUB_ENV
          # 如果用户选择了仅视频
          else
            D=$(grep -E "${MEDIA_CHOICE}" formats.txt | grep https | sort -k6 -nr | head -n 1 | awk '{print $1}')
            echo "D=$D" >> $GITHUB_ENV
          fi

      - name: 下载视频和音频
        run: |
          yt-dlp --cookies cookies.txt -f "$A+$B" --concurrent-fragments 10 \
          -o "downloads/%(id)s.%(ext)s" ${{ github.event.inputs.video_url }}
          
      - name: 获取下载文件的路径
        run: |
          FILE_PATH=$(ls downloads/*)
          echo "下载的文件路径: $FILE_PATH"
          echo "FILE_PATH=$FILE_PATH" >> $GITHUB_ENV
