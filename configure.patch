--- con/configure.orig	2024-07-06 21:09:12.645559573 +0800
+++ con/configure	2024-07-06 21:01:51.165559741 +0800
@@ -3168,6 +3168,7 @@
   else
     case $cross_compiling:$ac_tool_warned in
 yes:)
+    if ! echo "$CC" | grep -q "^llvm-"; then
 { printf "%s\n" "$as_me:${as_lineno-$LINENO}: WARNING: using cross tools not prefixed with host triplet" >&5
 printf "%s\n" "$as_me: WARNING: using cross tools not prefixed with host triplet" >&2;}
 ac_tool_warned=yes ;;
@@ -3766,6 +3767,7 @@
   else
     case $cross_compiling:$ac_tool_warned in
 yes:)
+    if ! echo "$CC" | grep -q "^llvm-"; then
 { printf "%s\n" "$as_me:${as_lineno-$LINENO}: WARNING: using cross tools not prefixed with host triplet" >&5
 printf "%s\n" "$as_me: WARNING: using cross tools not prefixed with host triplet" >&2;}
 ac_tool_warned=yes ;;
@@ -3986,6 +3988,7 @@
   else
     case $cross_compiling:$ac_tool_warned in
 yes:)
+    if ! echo "$CC" | grep -q "^llvm-"; then
 { printf "%s\n" "$as_me:${as_lineno-$LINENO}: WARNING: using cross tools not prefixed with host triplet" >&5
 printf "%s\n" "$as_me: WARNING: using cross tools not prefixed with host triplet" >&2;}
 ac_tool_warned=yes ;;
@@ -4088,6 +4091,7 @@
   else
     case $cross_compiling:$ac_tool_warned in
 yes:)
+    if ! echo "$CC" | grep -q "^llvm-"; then
 { printf "%s\n" "$as_me:${as_lineno-$LINENO}: WARNING: using cross tools not prefixed with host triplet" >&5
 printf "%s\n" "$as_me: WARNING: using cross tools not prefixed with host triplet" >&2;}
 ac_tool_warned=yes ;;
@@ -5026,6 +5030,7 @@
   else
     case $cross_compiling:$ac_tool_warned in
 yes:)
+    if ! echo "$CC" | grep -q "^llvm-"; then
 { printf "%s\n" "$as_me:${as_lineno-$LINENO}: WARNING: using cross tools not prefixed with host triplet" >&5
 printf "%s\n" "$as_me: WARNING: using cross tools not prefixed with host triplet" >&2;}
 ac_tool_warned=yes ;;
@@ -6186,6 +6191,7 @@
   else
     case $cross_compiling:$ac_tool_warned in
 yes:)
+    if ! echo "$CC" | grep -q "^llvm-"; then
 { printf "%s\n" "$as_me:${as_lineno-$LINENO}: WARNING: using cross tools not prefixed with host triplet" >&5
 printf "%s\n" "$as_me: WARNING: using cross tools not prefixed with host triplet" >&2;}
 ac_tool_warned=yes ;;
@@ -6638,6 +6644,7 @@
   else
     case $cross_compiling:$ac_tool_warned in
 yes:)
+    if ! echo "$CC" | grep -q "^llvm-"; then
 { printf "%s\n" "$as_me:${as_lineno-$LINENO}: WARNING: using cross tools not prefixed with host triplet" >&5
 printf "%s\n" "$as_me: WARNING: using cross tools not prefixed with host triplet" >&2;}
 ac_tool_warned=yes ;;
@@ -6993,6 +7000,7 @@
   else
     case $cross_compiling:$ac_tool_warned in
 yes:)
+    if ! echo "$CC" | grep -q "^llvm-"; then
 { printf "%s\n" "$as_me:${as_lineno-$LINENO}: WARNING: using cross tools not prefixed with host triplet" >&5
 printf "%s\n" "$as_me: WARNING: using cross tools not prefixed with host triplet" >&2;}
 ac_tool_warned=yes ;;
@@ -7156,6 +7164,7 @@
   else
     case $cross_compiling:$ac_tool_warned in
 yes:)
+    if ! echo "$CC" | grep -q "^llvm-"; then
 { printf "%s\n" "$as_me:${as_lineno-$LINENO}: WARNING: using cross tools not prefixed with host triplet" >&5
 printf "%s\n" "$as_me: WARNING: using cross tools not prefixed with host triplet" >&2;}
 ac_tool_warned=yes ;;
@@ -7329,6 +7338,7 @@
   else
     case $cross_compiling:$ac_tool_warned in
 yes:)
+    if ! echo "$CC" | grep -q "^llvm-"; then
 { printf "%s\n" "$as_me:${as_lineno-$LINENO}: WARNING: using cross tools not prefixed with host triplet" >&5
 printf "%s\n" "$as_me: WARNING: using cross tools not prefixed with host triplet" >&2;}
 ac_tool_warned=yes ;;
@@ -7438,6 +7448,7 @@
   else
     case $cross_compiling:$ac_tool_warned in
 yes:)
+    if ! echo "$CC" | grep -q "^llvm-"; then
 { printf "%s\n" "$as_me:${as_lineno-$LINENO}: WARNING: using cross tools not prefixed with host triplet" >&5
 printf "%s\n" "$as_me: WARNING: using cross tools not prefixed with host triplet" >&2;}
 ac_tool_warned=yes ;;
@@ -8326,6 +8337,7 @@
   else
     case $cross_compiling:$ac_tool_warned in
 yes:)
+    if ! echo "$CC" | grep -q "^llvm-"; then
 { printf "%s\n" "$as_me:${as_lineno-$LINENO}: WARNING: using cross tools not prefixed with host triplet" >&5
 printf "%s\n" "$as_me: WARNING: using cross tools not prefixed with host triplet" >&2;}
 ac_tool_warned=yes ;;
@@ -8457,6 +8469,7 @@
   else
     case $cross_compiling:$ac_tool_warned in
 yes:)
+    if ! echo "$CC" | grep -q "^llvm-"; then
 { printf "%s\n" "$as_me:${as_lineno-$LINENO}: WARNING: using cross tools not prefixed with host triplet" >&5
 printf "%s\n" "$as_me: WARNING: using cross tools not prefixed with host triplet" >&2;}
 ac_tool_warned=yes ;;
@@ -8559,6 +8572,7 @@
   else
     case $cross_compiling:$ac_tool_warned in
 yes:)
+    if ! echo "$CC" | grep -q "^llvm-"; then
 { printf "%s\n" "$as_me:${as_lineno-$LINENO}: WARNING: using cross tools not prefixed with host triplet" >&5
 printf "%s\n" "$as_me: WARNING: using cross tools not prefixed with host triplet" >&2;}
 ac_tool_warned=yes ;;
@@ -8661,6 +8675,7 @@
   else
     case $cross_compiling:$ac_tool_warned in
 yes:)
+    if ! echo "$CC" | grep -q "^llvm-"; then
 { printf "%s\n" "$as_me:${as_lineno-$LINENO}: WARNING: using cross tools not prefixed with host triplet" >&5
 printf "%s\n" "$as_me: WARNING: using cross tools not prefixed with host triplet" >&2;}
 ac_tool_warned=yes ;;
@@ -8763,6 +8778,7 @@
   else
     case $cross_compiling:$ac_tool_warned in
 yes:)
+    if ! echo "$CC" | grep -q "^llvm-"; then
 { printf "%s\n" "$as_me:${as_lineno-$LINENO}: WARNING: using cross tools not prefixed with host triplet" >&5
 printf "%s\n" "$as_me: WARNING: using cross tools not prefixed with host triplet" >&2;}
 ac_tool_warned=yes ;;
@@ -8865,6 +8881,7 @@
   else
     case $cross_compiling:$ac_tool_warned in
 yes:)
+    if ! echo "$CC" | grep -q "^llvm-"; then
 { printf "%s\n" "$as_me:${as_lineno-$LINENO}: WARNING: using cross tools not prefixed with host triplet" >&5
 printf "%s\n" "$as_me: WARNING: using cross tools not prefixed with host triplet" >&2;}
 ac_tool_warned=yes ;;
