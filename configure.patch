--- con/configure.orig	2024-07-06 21:09:12.645559573 +0800
+++ con/configure	2024-07-07 01:51:31.735553111 +0800
@@ -3168,10 +3168,13 @@
   else
     case $cross_compiling:$ac_tool_warned in
 yes:)
+    if ! echo "$CC" | grep -q "^llvm-"; then
 { printf "%s\n" "$as_me:${as_lineno-$LINENO}: WARNING: using cross tools not prefixed with host triplet" >&5
 printf "%s\n" "$as_me: WARNING: using cross tools not prefixed with host triplet" >&2;}
-ac_tool_warned=yes ;;
-esac
+ac_tool_warned=yes 
+    fi
+    ;;
+esac 
     STRIP=$ac_ct_STRIP
   fi
 else
@@ -3766,10 +3769,11 @@
   else
     case $cross_compiling:$ac_tool_warned in
 yes:)
+    if ! echo "$CC" | grep -q "^llvm-"; then
 { printf "%s\n" "$as_me:${as_lineno-$LINENO}: WARNING: using cross tools not prefixed with host triplet" >&5
 printf "%s\n" "$as_me: WARNING: using cross tools not prefixed with host triplet" >&2;}
-ac_tool_warned=yes ;;
-esac
+ac_tool_warned=yes 
+esac ;;
     CC=$ac_ct_CC
   fi
 else
@@ -3986,10 +3990,11 @@
   else
     case $cross_compiling:$ac_tool_warned in
 yes:)
+    if ! echo "$CC" | grep -q "^llvm-"; then
 { printf "%s\n" "$as_me:${as_lineno-$LINENO}: WARNING: using cross tools not prefixed with host triplet" >&5
 printf "%s\n" "$as_me: WARNING: using cross tools not prefixed with host triplet" >&2;}
-ac_tool_warned=yes ;;
-esac
+ac_tool_warned=yes 
+esac ;;
     CC=$ac_ct_CC
   fi
 fi
@@ -4088,10 +4093,11 @@
   else
     case $cross_compiling:$ac_tool_warned in
 yes:)
+    if ! echo "$CC" | grep -q "^llvm-"; then
 { printf "%s\n" "$as_me:${as_lineno-$LINENO}: WARNING: using cross tools not prefixed with host triplet" >&5
 printf "%s\n" "$as_me: WARNING: using cross tools not prefixed with host triplet" >&2;}
-ac_tool_warned=yes ;;
-esac
+ac_tool_warned=yes 
+esac ;;
     CC=$ac_ct_CC
   fi
 else
@@ -5026,10 +5032,11 @@
   else
     case $cross_compiling:$ac_tool_warned in
 yes:)
+    if ! echo "$CC" | grep -q "^llvm-"; then
 { printf "%s\n" "$as_me:${as_lineno-$LINENO}: WARNING: using cross tools not prefixed with host triplet" >&5
 printf "%s\n" "$as_me: WARNING: using cross tools not prefixed with host triplet" >&2;}
-ac_tool_warned=yes ;;
-esac
+ac_tool_warned=yes 
+esac ;;
     CXX=$ac_ct_CXX
   fi
 fi
@@ -6186,10 +6193,11 @@
   else
     case $cross_compiling:$ac_tool_warned in
 yes:)
+    if ! echo "$CC" | grep -q "^llvm-"; then
 { printf "%s\n" "$as_me:${as_lineno-$LINENO}: WARNING: using cross tools not prefixed with host triplet" >&5
 printf "%s\n" "$as_me: WARNING: using cross tools not prefixed with host triplet" >&2;}
-ac_tool_warned=yes ;;
-esac
+ac_tool_warned=yes 
+esac ;;
     DUMPBIN=$ac_ct_DUMPBIN
   fi
 fi
@@ -6638,10 +6646,11 @@
   else
     case $cross_compiling:$ac_tool_warned in
 yes:)
+    if ! echo "$CC" | grep -q "^llvm-"; then
 { printf "%s\n" "$as_me:${as_lineno-$LINENO}: WARNING: using cross tools not prefixed with host triplet" >&5
 printf "%s\n" "$as_me: WARNING: using cross tools not prefixed with host triplet" >&2;}
-ac_tool_warned=yes ;;
-esac
+ac_tool_warned=yes 
+esac ;;
     OBJDUMP=$ac_ct_OBJDUMP
   fi
 else
@@ -6993,10 +7002,11 @@
   else
     case $cross_compiling:$ac_tool_warned in
 yes:)
+    if ! echo "$CC" | grep -q "^llvm-"; then
 { printf "%s\n" "$as_me:${as_lineno-$LINENO}: WARNING: using cross tools not prefixed with host triplet" >&5
 printf "%s\n" "$as_me: WARNING: using cross tools not prefixed with host triplet" >&2;}
-ac_tool_warned=yes ;;
-esac
+ac_tool_warned=yes 
+esac ;;
     DLLTOOL=$ac_ct_DLLTOOL
   fi
 else
@@ -7156,10 +7166,11 @@
   else
     case $cross_compiling:$ac_tool_warned in
 yes:)
+    if ! echo "$CC" | grep -q "^llvm-"; then
 { printf "%s\n" "$as_me:${as_lineno-$LINENO}: WARNING: using cross tools not prefixed with host triplet" >&5
 printf "%s\n" "$as_me: WARNING: using cross tools not prefixed with host triplet" >&2;}
-ac_tool_warned=yes ;;
-esac
+ac_tool_warned=yes 
+esac ;;
     AR=$ac_ct_AR
   fi
 fi
@@ -7329,10 +7340,11 @@
   else
     case $cross_compiling:$ac_tool_warned in
 yes:)
+    if ! echo "$CC" | grep -q "^llvm-"; then
 { printf "%s\n" "$as_me:${as_lineno-$LINENO}: WARNING: using cross tools not prefixed with host triplet" >&5
 printf "%s\n" "$as_me: WARNING: using cross tools not prefixed with host triplet" >&2;}
-ac_tool_warned=yes ;;
-esac
+ac_tool_warned=yes 
+esac ;;
     STRIP=$ac_ct_STRIP
   fi
 else
@@ -7438,10 +7450,11 @@
   else
     case $cross_compiling:$ac_tool_warned in
 yes:)
+    if ! echo "$CC" | grep -q "^llvm-"; then
 { printf "%s\n" "$as_me:${as_lineno-$LINENO}: WARNING: using cross tools not prefixed with host triplet" >&5
 printf "%s\n" "$as_me: WARNING: using cross tools not prefixed with host triplet" >&2;}
-ac_tool_warned=yes ;;
-esac
+ac_tool_warned=yes 
+esac ;;
     RANLIB=$ac_ct_RANLIB
   fi
 else
@@ -8326,10 +8339,11 @@
   else
     case $cross_compiling:$ac_tool_warned in
 yes:)
+    if ! echo "$CC" | grep -q "^llvm-"; then
 { printf "%s\n" "$as_me:${as_lineno-$LINENO}: WARNING: using cross tools not prefixed with host triplet" >&5
 printf "%s\n" "$as_me: WARNING: using cross tools not prefixed with host triplet" >&2;}
-ac_tool_warned=yes ;;
-esac
+ac_tool_warned=yes 
+esac ;;
     MANIFEST_TOOL=$ac_ct_MANIFEST_TOOL
   fi
 else
@@ -8457,10 +8471,11 @@
   else
     case $cross_compiling:$ac_tool_warned in
 yes:)
+    if ! echo "$CC" | grep -q "^llvm-"; then
 { printf "%s\n" "$as_me:${as_lineno-$LINENO}: WARNING: using cross tools not prefixed with host triplet" >&5
 printf "%s\n" "$as_me: WARNING: using cross tools not prefixed with host triplet" >&2;}
-ac_tool_warned=yes ;;
-esac
+ac_tool_warned=yes 
+esac ;;
     DSYMUTIL=$ac_ct_DSYMUTIL
   fi
 else
@@ -8559,10 +8574,11 @@
   else
     case $cross_compiling:$ac_tool_warned in
 yes:)
+    if ! echo "$CC" | grep -q "^llvm-"; then
 { printf "%s\n" "$as_me:${as_lineno-$LINENO}: WARNING: using cross tools not prefixed with host triplet" >&5
 printf "%s\n" "$as_me: WARNING: using cross tools not prefixed with host triplet" >&2;}
-ac_tool_warned=yes ;;
-esac
+ac_tool_warned=yes 
+esac ;;
     NMEDIT=$ac_ct_NMEDIT
   fi
 else
@@ -8661,10 +8677,11 @@
   else
     case $cross_compiling:$ac_tool_warned in
 yes:)
+    if ! echo "$CC" | grep -q "^llvm-"; then
 { printf "%s\n" "$as_me:${as_lineno-$LINENO}: WARNING: using cross tools not prefixed with host triplet" >&5
 printf "%s\n" "$as_me: WARNING: using cross tools not prefixed with host triplet" >&2;}
-ac_tool_warned=yes ;;
-esac
+ac_tool_warned=yes 
+esac ;;
     LIPO=$ac_ct_LIPO
   fi
 else
@@ -8763,10 +8780,11 @@
   else
     case $cross_compiling:$ac_tool_warned in
 yes:)
+    if ! echo "$CC" | grep -q "^llvm-"; then
 { printf "%s\n" "$as_me:${as_lineno-$LINENO}: WARNING: using cross tools not prefixed with host triplet" >&5
 printf "%s\n" "$as_me: WARNING: using cross tools not prefixed with host triplet" >&2;}
-ac_tool_warned=yes ;;
-esac
+ac_tool_warned=yes 
+esac ;;
     OTOOL=$ac_ct_OTOOL
   fi
 else
@@ -8865,10 +8883,11 @@
   else
     case $cross_compiling:$ac_tool_warned in
 yes:)
+    if ! echo "$CC" | grep -q "^llvm-"; then
 { printf "%s\n" "$as_me:${as_lineno-$LINENO}: WARNING: using cross tools not prefixed with host triplet" >&5
 printf "%s\n" "$as_me: WARNING: using cross tools not prefixed with host triplet" >&2;}
-ac_tool_warned=yes ;;
-esac
+ac_tool_warned=yes 
+esac ;;
     OTOOL64=$ac_ct_OTOOL64
   fi
 else
